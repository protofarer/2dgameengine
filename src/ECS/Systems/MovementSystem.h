#pragma once

#include "../ECS.h"

class MovementSystem: public System {
	public:
		MovementSystem() {
			// TODO RequireComponent<...>()
		}

		void Update() {
			// for (auto entity: GetEntities()) {
			// TODO update entity sys based on velocity for every frame
			// }
		}
};